#include <iostream>
#include "TestP8.h"
#include "ThirdPriorityQueue.h"

using std::cout;

int main()
{
	testP8();
	
	/*
	ThirdPriorityQueue pq(rel2);
	pq.push(1, 1);
	pq.push(2, 2);
	pq.push(3, 3);
	pq.push(4, 4);
	pq.push(5, 5);
	pq.push(6, 6);
	pq.toString();
	cout << '\n';
	

	Element elem = pq.pop();
	//cout << pq.top().first << " " << pq.top().second() << '\n';
	cout << elem.first << " " << elem.second << '\n';
	*/

	return 0;
}